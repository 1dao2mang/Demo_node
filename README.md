# Demo Node.js with GitHub Actions CI/CD

This is a simple Node.js project to demonstrate Continuous Integration (CI) and Continuous Deployment (CD) using GitHub Actions.

## Workflows
- **CI**: Runs tests automatically on push or pull request to the `main` branch.
